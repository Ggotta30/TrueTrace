[pytest]
minversion = 7.0
addopts = -q -ra
testpaths = tests
python_files = test_*.py
markers =
    integration: integration tests that exercise the HTTP API and DB
    unit: fast unit tests
    flaky: marks tests that may occasionally fail under heavy load